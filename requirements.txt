# --- Core Framework (Cập nhật bản mới để hỗ trợ Python 3.13 tốt nhất) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.4.0

# --- Xử lý File & Môi trường ---
python-multipart>=0.0.18
python-dotenv>=1.0.1

# --- Giao tiếp Microservices ---
httpx>=0.27.2

# --- AI Integration Service ---
google-genai>=1.0.0

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.24.0

# --- OCR Grading Service (QUAN TRỌNG NHẤT: Bắt buộc bản mới để có Wheel cho 3.13) ---
opencv-python-headless>=4.10.0.84
numpy>=2.1.0              # Xử lý ma trận điểm ảnh cho OpenCV